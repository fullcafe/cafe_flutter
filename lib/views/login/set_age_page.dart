import 'package:flutter/material.dart';

class SetAgePage extends StatelessWidget {
  const SetAgePage({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Text('age');
  }
}
